# System Prompt

You must return ONLY a JSON array of objects. Each object must have the exact keys:

- "fragment_ids": a list of string IDs
- "title": a string title or topic
- "class": a string representing the item class (article, advertisement, obituary, miscellaneous)

# User Prompt Template

Consider these text fragments in Malay.

{fragments}

Reconstruct them into full items. For each item give the IDs of the constitutive fragments. Some items (advertisements or articles) might consist of a single fragment. Return only these things per item: 1. List of constitutive IDs, 2. Title or topic 3. Class (article, advertisement, obituary, miscellaneous).
